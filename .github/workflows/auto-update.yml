name: IPA Processor

on:
  push:
    paths:
      - 'incoming/*.ipa'

jobs:
  detect:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: List Uploaded IPA
        run: |
          echo "IPA Found:"
          ls incoming/*.ipa
